setwd("G:/Class Works/R-for-Data-Science-and-Machine-Learning-with-NBICT-Lab-B-8/RDSML-Day-55👉 Support Vector Regression in R (Part-3)")
library(e1071)
library(ggplot2)
library(dplyr)
dataset <- read.csv("day.csv")
bike <- dataset %>%
select(cnt, temp, atemp, hum, windspeed, season, yr, mnth,
holiday,weekday, workingday, weathersit)
str(bike)
bike <- bike %>%
mutate(
season = factor(season),
yr = factor(yr),
mnth = factor(mnth),
holiday = factor(holiday),
weekday = factor(weekday),
workingday = factor(holiday),
weathersit = factor(weathersit)
)
str(bike)
# Train/Test split # set.seed(123) dile bar baar same value asbe
set.seed(123)
n <- nrow(bike)
train_idx <- sample(indices, size = 0.7*n)
tranin_set <- bike[train_idx, ]
indices <- seq_len(n)
train_idx <- sample(indices, size = 0.7*n)
tranin_set <- bike[train_idx, ]
test_set <- bike[-train_idx, ]
